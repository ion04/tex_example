\documentclass{bmstu}

\newtheorem{solution}{Решение}

\begin{document}

    \makereporttitle
    {Информатика, искусственный интеллект и системы управления} % Название факультета
    {Компьютерные системы и сети} % Название кафедры
    {лабораторной работе №~1} % Название работы (в дат. падеже)
    {Технология разработки программных систем} % Название курса (необязательный аргумент)
    {Структуры и методы обработки данных} % Тема работы
    {11} % Номер варианта (необязательный аргумент)
    {Ионов~Н.~В./ИУ6-45Б} % Номер группы/ФИО студента (если авторов несколько, их необходимо разделить запятой)
    {Пугачев~Е.~К.} % ФИО преподавателя
    
    \chapter{Введение}

    \underline{\textbf{Цель лабораторной работы:}} исследование структур данных, методов их обработки и оценки.
    \vspace{8pt}

    \underline{\textbf{Задание.}}
    \begin{enumerate}
        \item Ознакомиться с теоретическими сведениями об абстрактных структурах данных и методах их обработки. 
        \item Для указанной задачи и структуры данных предложить способ реализации и определить требуемый объем памяти.
        \item Провести анализ заданных  методов поиска, упорядочения  и корректировки  данных.  Оценить  время  выполнения  соответствующих операций. 
        \item Предложить альтернативный вариант решения задачи, в котором должно  быть  минимум одно  улучшение. Улучшения  могут касаться  как  структуры  данных,  так  и  основных  операций.  Обосновать новые решения, используя количественные и качественные критерии.
    \end{enumerate}
    
    \begin{table}[H]
        \centering
        \small
        \begin{tabular}{|r|*{4}{c|}}
            \hline
            Задача & Структура данных & Поиск & Упорядовачиние & Корректировка \\
            \hline
            2 & Таблица & Вычисление адреса & Пузырьком & Удаление сдвигом \\
            \hline
        \end{tabular}
        \caption{Условие исходного задания}
    \end{table}

    \underline{\textbf{Задача.}}\vspace{3pt}

    Дана таблица  материальных нормативов, состоящая из $K$ записей фиксированной длины вида: код детали;
    код материала; единица измерения; номер цеха; норма расхода.\vspace{5pt}

    Лабораторная работа будет выполнена на языке \texttt{C}.
    \clearpage

    \chapter{Основная часть} 

    \section{Реализация}
    Для хранения таблицы будет использоваться статический массив длины $K$.
    Причём элементом этого массива будет запись \texttt{data}.

    \includeimage
        {vis_const_array} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (с обтеканием)
        {h} % Положение рисунка (см. wrapfigure из пакета wrapfig)
        {1\textwidth} % Ширина рисунка
        {Реализация хранения таблицы в виде статического массива} % Подпись рисунка

    Ниже представлен код, который реализует структуру хранения данных.

    \includelisting
        {def_struct.c}
        {Реализации структуры \texttt{data} и соответствующей таблицы}
    
    \clearpage
    \section{Определение объёма данных}

    \begin{itemize}
        \item Поскольку все поля --- \texttt{char}, структура имеет выравнивание 1.
         Это означает, что её размер равен сумме полей, т.е. 37 байт.
        \item Объем памяти, занимаемой таблицей, содержащим $K$ записей: $K \times 37$ байт.
    \end{itemize}

    \section{Анализ методов}
    Сначала рассмотрим алгоритм поиска -- \underline{\texttt{вычисление адреса}}.

    \includelisting
        {hash_func.c}
        {Реализации полиномиальной хэш-функции для вычисления адреса}

    Рассчитаем количество тактов для вычисления адреса.
    Пусть длина \texttt{unit\_code} равна $L$.
    \begin{gather*}
        T_{\texttt{hash}} = t_{\texttt{init}} + t_{\texttt{loop}} + t_{\texttt{return}} \\
        t_{\texttt{init}} = t_{=} + t_{=} = 2 + 2 = 4 \\ 
        t_{\texttt{return}} = t_{/} + t_{=} = 28 + 2 = 30
    \end{gather*}
    
    В языке \texttt{C} компиляторы \texttt{gcc, clang} используют ленивые вычисления для оператора \texttt{\&\&},
    поэтому поведение цикла зависит от значения прочитанного символа. Рассмотрим два случая.
    \begin{enumerate}
        \item $ c \neq 0$
            \begin{gather*}
                T(c = \texttt{*unit\_code++}) = t_{\wedge} + t_{++} + t_{=} = 2 + 1 + 2 = 5 \\
                T(\texttt{c \&\& c != '$\backslash$0'})_{\text{при } c \neq 0} = t_{+} + t_{\text{переход}} + t_{+} + t_{\&\&} = 2 + 1 + 2 + 1 = 6 \\
                \text{Итого на проверку условия: } 5 + 6 = 11 \\
                T(\texttt{hash = ((hash << 5) + hash) + c}) = t_{\rightarrow} + t_{+} + t_{+} + t_{=} = \\ = 1 + 2 + 2 + 2 = 7 \\
                t_{\texttt{iter}} = 18
            \end{gather*}
        \item $ c = 0$
         \begin{gather*}
            T(c = \texttt{*unit\_code++}) = 5 \\
            T(\texttt{c \&\& c != '$\backslash$0'})_{\text{при } c = 0} = t_{+} + t_{\text{переход}} = 2 + 1 = 3 \\
            \text{Тело цикла не выполняется} \\
            t_{\texttt{last}} = 8
         \end{gather*}
    \end{enumerate}

    Тогда 
    \begin{gather*}
        T_{\texttt{hash}} = t_{\texttt{iter}}\cdot L + t_{\texttt{last}} + t_{\texttt{init}} + t_{\texttt{return}} \\
        T_{\texttt{hash}} = 18\cdot L + 42
    \end{gather*} 

    Рассмотрим алгоритм упорядочивания -- \underline{\texttt{пузырьком}}.

    \includelisting
        {buble_sort.c}
        {Реализации упорядочения записей в таблице методом пузырьком}

    Оценка времени упорядочения структуры определяется формулой 
    \[ C_{\texttt{ср}} = \frac{K \cdot (K - 1)}{2}\]

    Рассмотрим алгоритм корректировки -- \underline{\texttt{удаление сдвигом}}.
    
    \includelisting
        {_remove.c}
        {Реализации корректировки записей в таблице методом удаление сдвигом}


    Рассчитаем количество тактов для корректировки.
    \begin{gather*}
        T_{\texttt{remove}} = t_{\texttt{init}} + t_{\texttt{loop}} + t_{\texttt{return}} \\
        t_{\texttt{init}} = t_{+} = 2 \\
        t_{\texttt{return}} = t_{+} + t_{=} = 2 + 2 = 4
    \end{gather*}
    Рассмотрим сколько тактов совершается за цикл
    \begin{gather*}
        T(\texttt{index + i < k - 1}) = t_{+} + t_{+} + t_{+} + 1 = 7 \\
        T(\texttt{data tmp = table[index + i]}) = 2 \\ 
        T(\texttt{table[index+i] = table[index + i + 1]}) = 2 \\
        T(\texttt{table[index + i + 1] = tmp}) = 2 \\
        t_{\texttt{iter}} = 7 + 3\cdot 2 + 1 = 14
    \end{gather*}

    Тогда
    \begin{gather*}
        t_{\texttt{loop}} = t_{\texttt{last}} + (k - \text{index} - 1)\cdot t_{\texttt{iter}} \\
        t_{\texttt{loop}} = 7 + (k - \text{index} - 1)\cdot 14 \\
        T_{\texttt{remove}} = 7 + (k - \text{index} - 1)\cdot 14 + 2 + 4 = 13 + (k - \text{index} - 1)\cdot 14
    \end{gather*}

    Рассчитаем среднее количество тактов

    \begin{gather*}
        T_{\text{ср}} = \frac{(k - 1 - (k - (k-1) - 1))\cdot 14}{2} = \\
        = \frac{(k-1)\cdot 14}{2} = (k-1)\cdot 7 \hspace{10pt} \text{тактов}
    \end{gather*}

    \clearpage

    \chapter{Альтернативная часть}

    \section{Предлагаемое улучшение}

    В качестве альтернативного решения будет использована сбалансированная структура данных -- \texttt{АВЛ-дерево}. 
    Данная структура позволит улучшить временные характеристики основных операций по сравнению с таблицей.

    \begin{table}[H]
        \centering
        \begin{tabular}{|l|c|c|c|}
            \hline
            \textbf{Операция} & \textbf{Исходное решение} & \textbf{Альтернативное решение} \\
            \hline
            Поиск & $O(1)$ & $O(\log K)$  \\
            \hline
            Упорядочивание & $O(K^2)$ & Не требуется \\
            \hline
            Удаление & $O(K)$ & $O(\log K)$ \\
            \hline
            Добавление & \textbf{---} & $O(\log K)$  \\
            \hline
        \end{tabular}
        \caption{Сравнение временных характеристик операций}
    \end{table}

    \textit{В АВЛ-дереве данные всегда хранятся в упорядоченном состоянии благодаря инварианту структуры.}

    \includeimage
        {vis_tree} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {1\textwidth} % Ширина рисунка
        {Структура дерева} % Подпись рисунка


    \section{Структура данных}

    Для хранения информации о материалах будет использоваться АВЛ-дерево, где ключом является код детали 
    \texttt{unit\_code}. Предполагается, что данные загружаются динамически.

    Ниже представлена реализация узла дерева:

    \includelisting
        {node_tree.c}
        {Структура узла АВЛ-дерева}

    \section{Вспомогательные функции}

    Для работы с АВЛ-деревом необходимы следующие вспомогательные функции:

    \includelisting
        {sub_funcs.c}
        {Функции для работы с высотой узлов}
    

    \section{Балансировка дерева}

    Для поддержания сбалансированности АВЛ-дерева используются малые и большие повороты:

    \includelisting
        {right_rotations.c}
        {Малый и большой левый поворот}

    \includeimage
        {ll} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Схема малого левого поворота (RR-rotation)} % Подпись рисунка

    \includeimage
        {rl} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {H} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Схема большого левого поворота (RL-rotation)} % Подпись рисунка

    
    \underline{Пояснение левых поворотов, на примере вставки узла}

    \includeimage
        {hint_rr_0} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Пояснение малого левого поворота (RR-rotation)} % Подпись рисунка

    \includeimage
        {hint_rr_gap} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Пояснение малого левого поворота (RR-rotation)}

    \includeimage
        {hint_rl_0} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {t} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Пояснение большого левого поворота (RL-rotation)} % Подпись рисунка

    \includeimage
        {hint_rl_gap} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {t} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Пояснение большого левого поворота (RL-rotation)}


   
    \clearpage
    \includelisting
        {left_rotations.c}
        {Малый и большой правый поворот}

    \includeimage
        {rr} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.7\textwidth} % Ширина рисунка
        {Схема малого правого поворота (LL-rotation)} % Подпись рисунка

    \includeimage
        {lr} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Схема большого правого поворота (LR-rotation)} % Подпись рисунка


    \section{Операции с деревом}

    \textbf{Поиск элемента:}

    \includelisting
        {find_node.c}
        {Функция поиска узла по коду детали}

    \textbf{Вставка элемента:}

    \includelisting
        {insert_tree.c}
        {Функция вставки узла в дерево}
    
    Рассмотрим пример вставки в АВЛ-дерево, но в качестве ключей будут целые числа.
    При удаления будет такая же структура узла, для наглядности.

    \includeimage
        {example_default} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Исходное дерево} % Подпись рисунка

    \includeimage
        {ex_insertion_0} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение вставки в самобалансирующиеся дерево} % Подпись рисунка

    \includeimage
        {ex_insertion_1} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение вставки в самобалансирующиеся дерево} % Подпись рисунка

    \includeimage
        {ex_insertion_2} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение вставки в самобалансирующиеся дерево} % Подпись рисунка

    \includeimage
        {ex_insertion_gap} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение вставки в самобалансирующиеся дерево} % Подпись рисунка

    \clearpage
    \textbf{Удаление элемента:}

    \includelisting
        {remove_node.c}
        {Функция удаления узла из дерева}

    \includeimage
        {ex_remove_0} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение удаления из самобалансирующегося дерева} % Подпись рисунка

    \includeimage
        {ex_remove_1} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение удаления из самобалансирующегося дерева} % Подпись рисунка

    \includeimage
        {ex_remove_2} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение удаления из самобалансирующегося дерева} % Подпись рисунка

    \includeimage
        {ex_remove_gap} % Имя файла без расширения (файл должен быть расположен в директории inc/img/)
        {f} % Обтекание (без обтекания)
        {h} % Положение рисунка (см. figure из пакета float)
        {0.8\textwidth} % Ширина рисунка
        {Пояснение удаления из самобалансирующегося дерева} % Подпись рисунка
    
    \clearpage
    \section{Оценка эффективности}

    Использование АВЛ-дерева вместо таблицы со статическим массивом даёт следующие преимущества:

    \begin{itemize}
        \item \textbf{Динамическое расширение:} отсутствие ограничения на максимальное количество записей $K$
        \item \textbf{Гарантированное время поиска:} $O(\log K)$ вместо ожидаемого $O(1)$ при хэшировании
        \item \textbf{Эффективная корректировка:} $O(\log K)$ против $O(K)$ при удалении сдвигом
        \item \textbf{Автоматическая упорядоченность:} данные всегда отсортированы по ключу
    \end{itemize}

    Недостатком является дополнительная память на хранение указателей и высоты узлов:
    \begin{itemize}
        \item Для $K$ узлов требуется дополнительно $K \cdot (2 \cdot 8 + 4)$ байт
        \item Временные затраты на балансировку при вставке и удалении
    \end{itemize}

    Таким образом, АВЛ-дерево является эффективной альтернативой для динамически изменяемых данных с частыми операциями поиска и корректировки.
    \clearpage

    \chapter{Заключение}

    \begin{table}[H]
        \centering
        \small
        \renewcommand{\arraystretch}{1.8}
        \begin{tabular}{|>{\centering\arraybackslash}p{4cm}|>{\centering\arraybackslash}p{2.5cm}|>{\centering\arraybackslash}p{2.5cm}|>{\centering\arraybackslash}p{3cm}|>{\centering\arraybackslash}p{3.1cm}|}
            \hline
            & \textbf{Структура данных} & \textbf{Алгоритм поиска} & \textbf{Алгоритм упорядочивания} & \textbf{Корректировка} \\
            \hline
            \textbf{Исходный вариант} & Статический массив & Вычисление адреса & Пузырьком & Удаление сдвигом \\
            \cline{2-5}
            & $37K$ байт & $18L + 42$ тактов & \(\displaystyle \frac{K(K-1)}{2}\)   среднее кол-во перестановок & $(K-1)\cdot 7$ среднее кол-во тактов\\
            \hline
            \textbf{Альтернативный вариант} & АВЛ-дерево & Бинарный поиск & Не требуется & Удаление с балансировкой \\
            \cline{2-5}
            & $57K$ байт & $\log_2 K$ среднее кол-во сравнений & Постоянно благодаря структуре дерева &  $2 \cdot \log_2 K$ операций \\
            \hline
        \end{tabular}
        \caption{Сравнительный анализ исходного и альтернативного решения}
    \end{table}

    \vspace{-0.3cm}
    \begin{flushleft}
        \textit{Примечание: $K$ \textbf{—} количество записей, $L$ \textbf{—} длина кода детали, $i$ \textbf{—} индекс удаляемого элемента.}
    \end{flushleft}

\section*{Анализ недостатков исходного решения}
При детальном рассмотрении метода вычисления адреса были выявлены следующие существенные недостатки:
\begin{itemize}
    \item \textbf{Наличие пустых ячеек:} Метод вычисления адреса редко дает идеальное распределение без коллизий. На практике массив таблицы содержит незаполненные ячейки, что приводит к неэффективному использованию памяти.
    \item \textbf{Коллизии:} При совпадении хэш-кодов для разных ключей возникает необходимость разрешения коллизий. Это может привести к значительному увеличению времени поиска.
    \item \textbf{Статический размер:} При переполнении таблицы требуется её полное перестроение.
\end{itemize}

\section*{Преимущества альтернативного решения}
\begin{itemize}
    \item \textbf{Динамическое расширение:} Отсутствие ограничения на максимальное количество записей; память выделяется по мере необходимости.
    \item \textbf{Гарантированная производительность:} Время выполнения основных операций (поиск, вставка, удаление) гарантированно составляет $O(\log K)$ в худшем случае.
    \item \textbf{Автоматическая упорядоченность:} Данные всегда хранятся в отсортированном виде по ключу, что упрощает выполнение операций вывода и поиска диапазонов. Операция упорядочивания не требуется.
    \item \textbf{Эффективная корректировка:} Удаление и добавление записей выполняется за логарифмическое время.
\end{itemize}

\section*{Недостатки альтернативного решения}
\begin{itemize}
    \item \textbf{Дополнительные накладные расходы по памяти:} На каждый узел требуются указатели на левое и правое поддеревья ($2 \cdot 8$ байт) и хранение высоты, что при больших $K$ приводит к увеличению потребляемой памяти по сравнению с массивом.
    \item \textbf{Сложность реализации:} Поддержка баланса требует реализации механизма поворотов.
    \item \textbf{Константное время:} Несмотря на логарифмическую асимптотику, операции с АВЛ-деревом имеют более высокие константные затраты, чем простое обращение по индексу в массиве.
\end{itemize}

Таким образом, выбор между статической таблицей и АВЛ-деревом зависит от условий эксплуатации.
Если данные статичны, известны заранее и критически важна максимальная скорость доступа при минимальных накладных расходах — предпочтительнее использовать таблицу с функцией поиска - вычисление адреса.
Если же данные динамически изменяются, требуется гарантированное время отклика и поддержка упорядоченности, то АВЛ-дерево является более эффективным и надежным решением.


\end{document}